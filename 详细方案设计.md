# Git AI Commit Message（AI 提交信息助手）详细方案设计

> 技术栈：Electron + Vue 3 + Vite + TypeScript + Naive UI + Pinia
> 文档目标：可直接据此进入编码实施，覆盖模块划分、交互流程、UI 搭建逻辑与细节
> 关联文档：`可行性分析.md`

---

## 一、应用定位与核心工作流

### 1.1 一句话定位
一个本地优先的桌面端 Git 助手：选项目 → 看改动 → 暂存 → 让 DeepSeek 写 commit message → 确认 → 提交/推送。

### 1.2 主流程（Happy Path）

```
启动 → 项目列表 → 选中项目 → 工作台
                              │
            ┌─────────────────┼──────────────────┐
            ▼                 ▼                  ▼
        文件变更面板        单文件 diff        AI 提交面板
            │                                     │
        勾选 → 暂存 ──────────────► [生成 message]
                                              │ 流式输出
                                              ▼
                                        预览 + 二次确认
                                              │
                                     [确认提交] [重新生成] [手动改]
                                              │
                                        commit (→ push)
```

---

## 二、技术栈与依赖（最终确认）

| 层 | 选型 |
| --- | --- |
| 运行时 | Electron 30+ / Node 20（主进程） |
| 构建 | Vite 5 + `electron-vite` |
| 前端 | Vue 3.4+ + TypeScript 5 |
| UI 组件 | **Naive UI 2.x** |
| 状态管理 | Pinia 2 |
| Git 操作 | `simple-git` |
| AI 调用 | Node 内置 `fetch`（流式） |
| 持久化 | `electron-store` |
| diff 渲染 | `diff2html` + `highlight.js` |
| 打包 | `electron-builder` |

> 不引入 `vue-router`：视图切换少，用 Pinia 里的 `activeView` 状态控制即可，避免桌面端路由历史带来的副作用。

---

## 三、应用整体架构

### 3.1 进程与目录结构

```
git-ai-commit-message/
├── electron.vite.config.ts
├── electron-builder.yml
├── package.json
├── src/
│   ├── main/                       # 主进程
│   │   ├── index.ts                # 入口：创建窗口、注册 IPC
│   │   ├── ipc/
│   │   │   ├── project.ts          # 项目增删查 IPC handler
│   │   │   ├── git.ts              # git 操作 IPC handler
│   │   │   └── ai.ts               # deepseek 流式 handler
│   │   └── services/
│   │       ├── GitService.ts       # 封装 simple-git
│   │       ├── AiService.ts        # 封装 deepseek 流式
│   │       └── StoreService.ts     # electron-store 封装
│   │
│   ├── preload/                    # 预加载（沙箱桥）
│   │   ├── index.ts                # contextBridge.exposeInMainWorld('api', ...)
│   │   └── api.d.ts                # window.api 类型声明
│   │
│   └── renderer/                   # 渲染进程（Vue 应用）
│       ├── index.html
│       ├── main.ts
│       ├── App.vue
│       ├── components/             # 通用组件
│       ├── views/                  # 视图（见第六节）
│       ├── stores/                 # Pinia
│       ├── composables/            # useGitStream / useTheme 等
│       └── assets/
```

### 3.2 进程通信契约（主 ↔ 渲染）

通过 `contextBridge` 暴露 `window.api`，**只暴露具名方法，不暴露 ipcRenderer**：

```ts
// preload/api.d.ts
export interface ProjectMeta { id: string; name: string; path: string; createdAt: number }

export interface GitApi {
  listProjects(): Promise<ProjectMeta[]>;
  addProject(path: string): Promise<ProjectMeta>;       // 内部校验是否 git 仓库
  removeProject(id: string): Promise<void>;

  status(path: string): Promise<StatusResult>;
  log(path: string, opts?: { maxCount?: number }): Promise<LogResult>;
  diffFile(path: string, file: string, staged: boolean): Promise<string>;
  diffStaged(path: string): Promise<string>;            // 喂给 AI
  add(path: string, files: string[]): Promise<void>;
  reset(path: string, files: string[]): Promise<void>;
  commit(path: string, message: string): Promise<string>;
  push(path: string, remote?: string, branch?: string): Promise<void>;
}

export interface AiApi {
  generateCommitMessage(input: { projectPath: string; extra?: string }): Promise<void>;
  onChunk(cb: (delta: string) => void): () => void;     // 返回取消订阅
  onDone(cb: () => void): () => void;
  onError(cb: (err: { message: string }) => void): () => void;
  abort(): Promise<void>;                                // 中断生成

  getApiKey(): Promise<string | null>;
  setApiKey(key: string): Promise<void>;
}
```

---

## 四、模块划分总览

| 模块 | 核心职责 | 主入口 |
| --- | --- | --- |
| **M1 项目管理** | 项目文件夹增删、持久化、当前选中态 | 左侧 `ProjectSider` |
| **M2 Git 工作台** | 文件变更、暂存、单文件 diff、提交历史 | 中部 `Workspace` |
| **M3 AI 提交** | 触发生成、流式渲染、二次确认、commit/push | 右侧 `AiCommitPanel` |
| **M4 设置** | API Key、语言/风格偏好、主题 | 顶栏齿轮 → `SettingsModal` |
| **M5 应用外壳** | 窗口、顶栏、主题、全局消息 | `AppLayout` |

---

## 五、各模块详解（功能 + 交互 + UI）

### M1 · 项目管理

#### 功能清单
1. 读取项目列表（启动时从 electron-store 恢复）。
2. **添加项目**：点"添加" → 调系统目录选择对话框 → 主进程校验是否 Git 仓库 → 写入持久化 → 列表追加。
3. **删除项目**：列表项右键/删除按钮 → 确认弹窗 → 仅从应用列表移除，**不动磁盘**。
4. **选中项目**：单击进入该项目的 Git 工作台（M2）。
5. **重命名**（可选）：仅改应用内显示名，不影响真实目录名。
6. 非法状态提示：路径被删/移动、不再是 Git 仓库时，列表项标灰并提示。

#### 交互流程
```
[+ 添加项目] → 文件夹选择 → 校验 git → 成功入列 / 失败提示
[项目项 hover] → 显示「删除」图标 → 二次确认 → 移除
[项目项 click] → activeProjectId 变更 → 切到 Workspace 视图
```

#### UI 设计
- 左侧 `ProjectSider`，宽度 240px，可折叠。
- 顶部应用名 + 折叠按钮。
- 中部 `n-scrollbar` 包裹 `nThing` 或自定义项目卡片列表：
  - 每项：项目图标（`n-icon` 文件夹）+ 显示名 + 灰色短路径（截断）+ 选中态高亮。
  - hover 出现删除（`trash-outline`）与重命名图标。
- 底部固定的 `+ 添加项目` 主按钮。
- 空列表态：`n-empty` 插画 + "添加你的第一个项目"按钮。

---

### M2 · Git 工作台（核心）

#### 功能清单
1. **文件状态面板**：列出工作区改动文件，分三类——已暂存（staged）、已修改未暂存（modified）、未跟踪（untracked）。
2. **单文件 diff 视图**：点选文件 → 右侧展示该文件 diff（可切换 工作区态/暂存区态）。
3. **暂存 / 取消暂存**：单文件按钮、批量按钮（全暂存 / 全取消）。
4. **提交历史**：展示最近 N 条 commit（hash、作者、相对时间、message）。
5. **当前分支**：顶部显示分支名 + ahead/behind 计数（如可获取）。

#### 文件状态分类与图标映射（Naive UI tag 配色）
| git 状态 | 标签 | 颜色 | 行为 |
| --- | --- | --- | --- |
| staged（已暂存） | `已暂存` | success 绿 | 显示「取消暂存」 |
| modified（已修改） | `已修改` | warning 橙 | 显示「暂存」 |
| untracked（未跟踪） | `未跟踪` | info 蓝 | 显示「暂存」 |
| deleted | `已删除` | error 红 | 显示「暂存」 |
| renamed | `已重命名` | — | 显示「暂存」 |

#### 交互流程
**暂存与查看 diff：**
```
文件项 click → 加载该文件 diff → 渲染到右栏
[暂存] / [取消暂存] → 调 git.add/reset → 刷新 status → 文件在两栏间"移动"
顶部「全部暂存」 → git.add('.') → 刷新
```

**单文件 diff 切换：**
- 文件项点击后默认显示「工作区 vs HEAD」diff；
- 若该文件已暂存，提供 `n-tabs` 切换「工作区改动 / 已暂存改动」两态。
- 渲染：`diff2html` 生成 HTML，注入到 `n-injection`/`div`，配 highlight.js 主题。

**提交历史：**
- 工作台底部 `n-collapse` 折叠面板「最近提交」默认折叠；
- 展开后 `n-scrollbar` 纵向列表，每行：短 hash（`n-tag` 等宽）+ message（首行）+ 相对时间。

#### UI 布局
```
Workspace
├── 顶栏：[当前分支 tag] [ahead/behind] ............ [刷新]
├── 主体（n-split 水平两列，可拖拽）
│    ├── 左：FileStatusPanel（文件列表）
│    │     · 分组：已暂存 / 未暂存（n-divider 分隔）
│    │     · 每行：状态点 + 文件名 + 相对路径 + 操作按钮
│    │     · 顶部工具行：[全部暂存] [全部取消] [筛选]
│    └── 右：DiffViewer
│          · 头部：文件名 + [工作区/已暂存] 切换 + [行内/并排] 切换
│          · 主体：diff2html 渲染
└── 底部：CommitHistory（折叠面板）
```

#### 文件列表的暂存数量徽章
- 工作台顶栏放一个 `n-badge` 显示当前暂存文件数，点击滚动定位到 AI 面板的"生成"按钮（呼应 M3）。

---

### M3 · AI 提交信息（核心）

#### 功能清单
1. **触发生成**：暂存内容非空时「生成 message」按钮可用；点击 → 主进程抓取 staged diff → 调 DeepSeek 流式。
2. **流式渲染**：渲染区以"打字机"效果逐字追加；底部 `n-progress`（不确定态）或脉冲点表示生成中。
3. **中断**：生成中「生成」按钮变为「停止」，可中止（主进程 AbortController）。
4. **二次确认编辑**：生成完成后 message 进入可编辑 `n-input`（textarea，等宽字体）；用户可改写。
5. **重新生成**：保留同一份 diff 重新请求；可附带"风格"偏好（Conventional / 中文 / 详细）。
6. **提交**：确认 message → `git.commit`；可选勾选「提交并 push」。
7. **失败处理**：网络/鉴权错误 → 红色 `n-alert` + 「重试」；diff 过大警告。

#### 交互流程（状态机）
```
idle  ──[点生成]──► generating（流式追加）
                        │
              ┌─────────┼──────────┐
              │[停止]    │流结束     │出错
              ▼         ▼          ▼
            idle      editing    error（n-alert + 重试）
                        │
              ┌─────────┼──────────┐
              │[重新生成]│[确认提交] │[手动改]
              ▼         ▼          ▼
          generating  committing  editing（继续编辑）
                        │
                   commit 完成 → success toast → 刷新 status/log
                        │
                   (若勾 push) → push → 结果反馈
```

#### UI 设计
```
AiCommitPanel（右侧固定 360–420px，可折叠）
├── 头部：标题 + 暂存文件数徽章
├── 生成状态：
│     · idle: 大按钮 [✨ 生成提交信息]
│     · generating: n-spin + 流式文本（只读，等宽，逐字）
│     · editing: n-input type=textarea（可编辑，行高 1.6）
├── 选项行：
│     · n-radio-group 风格：[Conventional | 中文 | 详细]
│     · n-checkbox「提交并 push」
├── 操作行（editing 态显示）：
│     [确认提交](primary) [重新生成] [清空]
└── 提示区：
      · diff 过大 → n-alert warning「差异较大，已自动截断」
      · 生成失败 → n-alert error + 重试
```

#### 提示词（system）约束
固定 system message，强制输出格式（Conventional Commits），例如：

> 你是 commit message 专家。根据 diff 生成简洁 commit message，遵循 Conventional Commits（type: subject）。只输出最终 message，不要解释。

用户偏好（中文/详细）会动态拼到 system 或 user message。

#### 大 diff 截断策略
- 统计 staged diff 字符数，超阈值（如 ~12k 字符 ≈ 安全 token 预算）时：
  - 优先折叠/剔除二进制与超大文件 diff，保留文件名 + 增删统计行；
  - 仍超限则整体截断并在 UI 用 `n-alert` 提示"已截断"。

---

### M4 · 设置

#### 功能清单
1. **API Key 管理**：输入（密码框遮蔽）→ 测试连通性（发一条最小请求）→ 保存到 electron-store（主进程）。
2. **默认提交风格**：Conventional / 中文 / 详细（写 M3 默认值）。
3. **模型选择**：`deepseek-chat` / `deepseek-reasoner`（可扩展）。
4. **主题**：跟随系统 / 浅色 / 深色。
5. **关于**：版本、Git 检测状态、打开应用数据目录。

#### UI 设计
- 顶栏齿轮 → `n-modal`（preset=card，宽 520px）。
- 表单用 `n-form`：
  - API Key：`n-input type=password` + 「测试」按钮 + 连通状态 `n-tag`。
  - 风格、模型：`n-select`。
  - 主题：`n-radio-group`，实时切换。
- 底部「保存」/「取消」。

---

### M5 · 应用外壳

#### 功能清单
1. 自定义标题栏（`frame: false` + 自绘）或系统默认栏（推荐默认栏，省事）。
2. 顶栏：应用名 + 当前项目名 + 分支 + 设置齿轮 + 主题切换。
3. 全局 `n-message-provider`、`n-dialog-provider`、`n-notification-provider`、`n-config-provider`（主题与语言）。
4. 启动自检：检测系统 git 是否存在；缺失则全局通知 + 禁用工作台。

---

## 六、前端 UI 搭建逻辑与细节

### 6.1 视图（views）划分

| 视图 | 组件 | 触发条件 |
| --- | --- | --- |
| `WelcomeView` | 空状态引导 | 未选中任何项目 |
| `WorkspaceView` | 三栏工作台 | 已选中项目 |
| `SettingsModal` | 设置弹层（非路由视图） | 点齿轮 |

**视图切换不走路由**，由 `useUiStore.activeView` 控制；`App.vue` 用 `<component :is>` 渲染。

### 6.2 全局布局结构

```vue
<!-- App.vue 结构示意 -->
<n-config-provider :theme :locale>
  <n-message-provider>
    <n-dialog-provider>
      <n-notification-provider>
        <n-layout has-sider class="app-root">
          <!-- 左：项目侧栏 -->
          <n-layout-sider :width="240" :collapsed-width="48" v-model:collapsed="collapsed" bordered>
            <ProjectSider />
          </n-layout-sider>

          <!-- 右：主区 -->
          <n-layout>
            <n-layout-header bordered>
              <AppHeader />           <!-- 项目名/分支/齿轮/主题 -->
            </n-layout-header>
            <n-layout-content>
              <component :is="currentView" />
            </n-layout-content>
          </n-layout>
        </n-layout>

        <SettingsModal v-model:show="settingsOpen" />
      </n-notification-provider>
    </n-dialog-provider>
  </n-message-provider>
</n-config-provider>
```

> 工作台内部的"文件列表 + diff + AI 面板"三栏，在 `WorkspaceView` 内用第二个 `n-layout`（嵌套 `n-layout-sider` 右侧放 AI 面板）或 `n-split` 实现。

### 6.3 组件树

```
App.vue
├── AppHeader                     顶栏
├── ProjectSider                  项目侧栏
│   ├── ProjectItem * N
│   └── AddProjectButton
├── WelcomeView                   空态
└── WorkspaceView                 工作台
    ├── WorkspaceToolbar          分支/刷新/暂存数
    ├── FileStatusPanel           文件状态（左）
    │   ├── FileGroup (staged)
    │   ├── FileGroup (unstaged)
    │   └── FileRow * N
    ├── DiffViewer                diff（中/右）
    │   ├── DiffToolbar           文件名/态切换/视图切换
    │   └── DiffContent           diff2html 容器
    ├── CommitHistory             提交历史（底部折叠）
    └── AiCommitPanel             AI 面板（右侧栏）
        ├── GenerateButton
        ├── StreamingView
        ├── MessageEditor
        └── ActionRow
└── SettingsModal                 设置弹窗
```

### 6.4 Pinia Store 设计

**`useProjectStore`**
```ts
state: {
  projects: ProjectMeta[];        // 全部项目
  activeId: string | null;        // 当前项目
}
getters: { activeProject }
actions: { load(), add(path), remove(id), select(id) }
```

**`useGitStore`**（按当前项目）
```ts
state: {
  branch: string;
  status: StatusResult;           // staged/modified/untracked 分组后
  selectedFile: string | null;
  selectedStaged: boolean;        // 看工作区态 or 暂存态
  diffHtml: string;               // 渲染后 HTML
  logs: LogEntry[];
  loadingDiff: boolean;
}
actions: { refresh(), stage(files), unstage(files), diff(file, staged), commit(msg), push() }
// 所有 action 内部调 window.api，并落回 state
```

**`useAiStore`**
```ts
state: {
  phase: 'idle' | 'generating' | 'editing' | 'committing' | 'error';
  message: string;                // 流式累积 → 可编辑
  style: 'conventional' | 'zh' | 'detailed';
  autoPush: boolean;
  error: string | null;
  truncated: boolean;             // diff 截断提示
}
actions: {
  generate(), abort(), regenerate(),
  commit()                        // 确认提交（联动 gitStore）
}
```

**`useSettingsStore`**
```ts
state: { apiKey, model, defaultStyle, themeMode }
actions: { load(), save(), testConnection() }
```

**`useUiStore`**
```ts
state: { activeView: 'welcome' | 'workspace', siderCollapsed, settingsOpen, gitAvailable }
```

### 6.5 关键交互细节（容易踩坑点）

1. **流式渲染性能**：每个 chunk 都 `message += delta` 会高频触发响应式。用 `ref<string>` + 节流（如每 30ms 批量 flush 一次到视图），或用 `shallowRef` + 手动触发更新，避免逐字符重渲染整棵组件。
2. **AI 订阅生命周期**：`onChunk/onDone/onError` 返回取消订阅函数，在 `AiCommitPanel` 的 `onUnmounted` 调用，避免切换项目后旧回调仍写新 store。
3. **切换项目清场**：`activeId` 变化时，`gitStore.$reset()` + `aiStore.$reset()`，并 `abort()` 进行中的生成。
4. **diff 渲染时机**：`diff2html` 是同步重活，大 diff 会卡顿；在 `requestAnimationFrame` 或 `setTimeout(0)` 里执行，并给容器加骨架/`n-spin`。
5. **暂存与 AI 联动**：M2 暂存变化后，向 M3 投递"暂存数"与"是否有内容"信号；暂存为空时禁用「生成」并提示「请先暂存文件」。
6. **快捷操作**：
   - `Ctrl/Cmd+Enter`：在 editing 态确认提交。
   - `Ctrl/Cmd+G`：触发生成。
   - `Ctrl/Cmd+S`：暂存当前选中文件（覆盖浏览器默认保存）。
7. **空态与错误态**：每个面板都要有 `n-empty` / `n-result`，避免白屏。
8. **窗口尺寸**：默认 1200×780，最小 960×640；三栏在中后两栏用 `n-split` 保证小屏可拖。
9. **主题与代码配色**：dark 模式下 diff2html 注入 `diff2html-dark.min.css`；切主题时同步切换样式表。
10. **响应式来源**：electron `nativeTheme` 监听系统暗色变化 → 同步到 Naive `darkTheme`。

### 6.6 Naive UI 组件映射速查

| 场景 | 组件 |
| --- | --- |
| 全局布局 | `n-layout` / `n-layout-sider` / `n-layout-header` / `n-layout-content` |
| 可拖拽分栏 | `n-split` |
| 项目/文件列表 | 自定义列表 + `n-scrollbar` + `n-thing` / `n-list` |
| 滚动容器 | `n-scrollbar` |
| 文件状态标签 | `n-tag`（type: success/warning/info/error） |
| 徽章计数 | `n-badge` |
| 按钮/工具组 | `n-button` / `n-button-group` / `n-space` |
| 分支/态切换 | `n-tabs` / `n-radio-group` |
| 提交 message 输入 | `n-input type=textarea`（等宽 `font-family: monospace`） |
| 风格/模型选择 | `n-select` |
| 生成中反馈 | `n-spin` / `n-progress`(indeterminate) |
| 提示与反馈 | `n-alert` / `n-message` / `n-notification` / `n-dialog` |
| 设置弹窗 | `n-modal preset=card` + `n-form` |
| 折叠历史 | `n-collapse` / `n-collapse-item` |
| 空态 | `n-empty` |
| 主题/语言 | `n-config-provider` |
| 图标 | `n-icon` + `@vicons/ionicons5` |

---

## 七、视觉与主题

- **色板**：以 Naive 默认主题为基础，主色用 Git 友好的橙红（`#f1502f` Git 官方色）或品牌蓝；success/warning/error 沿用 Naive 语义色。
- **代码字体**：`'JetBrains Mono', 'Fira Code', Consolas, monospace`。
- **圆角与间距**：全局 `border-radius: 6px`，面板内边距 16px，列表行高 36px。
- **深浅色**：默认跟随系统；diff 区域始终高对比，便于阅读。
- **微交互**：暂存/取消暂存时文件行用 `n-` 内置 transition 平滑在两栏间过渡；AI 流式文字光标用闪烁竖线。

---

## 八、边界与异常处理汇总

| 场景 | 处理 |
| --- | --- |
| 用户未安装 Git | 启动自检失败 → 全局通知 + 工作台禁用 + 引导链接 |
| 添加非 Git 目录 | `checkIsRepo` 失败 → 表单内联报错"该目录不是 Git 仓库" |
| 项目路径失效（被删/移动） | 列表项标灰 + tooltip + 提供「移除」「重新定位」 |
| 暂存为空点生成 | 按钮禁用，hover 提示"请先暂存文件" |
| diff 过大 | 截断 + `n-alert` 警告，仍可生成 |
| API Key 缺失 | 首次生成时弹设置引导；保存后继续 |
| 生成失败（鉴权/网络） | `n-alert error` + 重试；保留已暂存状态 |
| 流式中断 | 记录已得内容，按钮回 idle，提示"已停止" |
| commit 失败（如 hooks 钩子） | 透传 git 错误信息到 `n-message` error |
| push 失败（无凭证/upstream） | 提示"请在终端执行一次 push 以配置凭证"，不阻塞 commit |
| 并发生成 | 同一项目只允许一个进行中流；切换项目自动 abort |

---

## 九、实施里程碑（可直接照此排期）

| 阶段 | 产出 | 预估 |
| --- | --- | --- |
| M0 脚手架 | electron-vite + Vue3 + TS + Naive UI 接入，IPC hello world | 0.5d |
| M1 项目管理 | ProjectSider + electron-store 持久化 + 增删选 | 0.5d |
| M2 Git 工作台 | status/log/add/reset + FileStatusPanel + DiffViewer | 1.5d |
| M3 AI 面板 | DeepSeek 流式 + 二次确认 + commit + push | 1.5d |
| M4 设置 + 自检 | API Key/风格/主题 + Git 检测 | 0.5d |
| M5 打磨与打包 | 异常态、空态、主题、electron-builder 出包 | 1d |

**合计约 5.5 人日** 出可用 MVP。

---

## 十、待你确认的开放项（实施时再定也可）

1. **应用名**：暂用 "Git Commit"，是否要个更品牌化的名字？
2. **标题栏**：用系统默认栏（省事）还是无边框自绘栏（更好看但要写拖拽/按钮）？默认推荐**系统默认栏**。
3. **窗口最小尺寸**：默认 960×640，是否合适？
4. **diff 视图**：默认行内（unified）还是并排（split）？默认推荐 **unified**，小屏更友好。

如无异议，下一步可直接进入 **M0 脚手架搭建**。
